# LLM + embeddings (local, via Ollama HTTP API)
ollama==0.4.4

# Vector store
chromadb==0.6.3

# Tracing + observability
# Phoenix runs via Docker: docker run -p 6006:6006 -p 4317:4317 arizephoenix/phoenix:latest
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp-proto-grpc

# Evaluation (uses qwen2.5:3b as judge, separate from generation model)
ragas==0.2.10
langchain-openai     # RAGAS judge LLM + embeddings wrapper
datasets             # RAGAS Dataset.from_dict()

# UI
gradio==6.14.0

# Data processing
pandas==2.2.3
kaggle==1.7.4
tqdm==4.67.1
